{
  "name": "doospace",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:client": "bun run --filter @doospace/client dev",
    "dev:server": "bun run --filter @doospace/server dev",
    "build:client": "bun run --filter @doospace/client build",
    "build:server": "bun run --filter @doospace/server build"
  }
}
